# lowfat-pantry — Session State / Pending Work

Resume point. This repo is **chakrit/lowfat-pantry**: a standalone `/lowfat` skill plus a
pantry of lowfat `.lf` filter plugins, replacing RTK.

## Deploy tier + full audit (2026-06-10, long session)

**51 community plugins** now (was 47). Built with REAL docker-captured samples:
- `redis-cli` (live redis:7-alpine: INFO/scan/keys/slowlog/errors; NB `ERR` replies exit 0)
- `pulumi` (pulumi-base + local file backend + random provider, 30-res stack; tail-anchored:
  verdict at END, `up` emits two `Resources:` sections — head/split cuts the wrong half)
- `wrangler` (4.99.0 on node:22; boilerplate-strip only — payload already terse)
- `az` (azure-cli 2.87.0; flag-driven: help 1.3k→60, JSON byte-exact, errors terse)
- **Deferred permanently-ish**: `vercel`/`netlify`/`flyctl`/`gt` — signal output is
  auth-gated; no real capture without accounts (backlog updated).

**Audit of all 51** (4 Explore-agent fan-out, findings hand-verified — about half were
false positives; verify before fixing). Fixed: dotnet raw-on-failure (pitfall 3) +
keep-list missed vstest failure blocks; gradlew `or-shell: cat` uncapped; rsync lite
head-cut end-anchored stats; prisma doubled drop; tests.yml failure cases silently
validated as exit 0 (pytest/mypy/ruff/curl — exit codes added); + real kubectl
no-cluster + go-test-pass captures. Readability comments on monster keep-lists.
Full suite green: 0 parse failures, 0 flags.

Docker hygiene: all lf-* containers removed; session-pulled images removed (node:22 kept —
may have pre-existed). Machine env untouched otherwise.

## Self-dogfood setup (2026-06-10) — ran `/lowfat` flow against this repo

This repo now eats its own dog food. State:
- **Synced** 9 toolchain-matched pantry plugins as symlinks into `~/.lowfat/plugins/<cat>/`:
  `rg gh jq diff curl wget make env tar` (`lowfat plugin list` shows them as community).
  Dropped `json-compact` — it intercepts a literal `json` binary that doesn't exist here,
  so it never fires (a no-op isn't dogfood). Scope chosen: toolchain-matched, not all 47.
- **Hook**: PreToolUse wired **project-locally** in `.claude/settings.local.json`
  (gitignored) running `lowfat hook` — rewrites intercepted Bash cmds to `lowfat <cmd>`.
  Verified: `rg …` → `lowfat rg …`; `echo` passes through. Effective **next session**
  (hooks load at startup). NB: this is a *project-local* variant of SKILL.md step 5, which
  only documents the user-scope hook — candidate skill enhancement (note both scopes).
- **⚠ PENDING — user must run** (trust is user-only, the drift guard): trust the 9 synced
  plugins, else the hook rewrites but lowfat won't execute the filter:
  ```sh
  for p in ~/.lowfat/plugins/*/*; do lowfat plugin trust "$(basename "$p")"; done
  ```
  Optional: `lowfat plugin untrust demo-compact` clears a stale leftover in trusted.toml.
- Uncommitted: `M .gitignore` (added `.inbox.log` for the ace-connect bridge). Intentional.

## Where we are (overnight autonomous build, resumed post-reset 2026-06-09)

**47 community plugins built, validated, and committed** (~30 commits across the session).
Full suite passes the rigorous `tests.yml`-aware validator: 0 parse failures, 0 over-prune/
non-compacting flags. Skill entrypoint, docs, READMEs, and two validators are in place.

### Built + committed (47 community; + 6 bundled = 53 commands)
VCS/CI: `rg gh glab` · Rust: `cargo` · TS/JS: `tsc eslint prettier npm pnpm yarn bun` ·
Python: `pytest ruff mypy black pip` · Go: `go golangci-lint` · .NET: `dotnet` · JVM:
`mvn gradlew` · Infra/ops: `kubectl helm terraform ansible-playbook systemctl journalctl
docker-compose ssh rsync` · Cloud/data: `aws gcloud psql sqlite3 env` · Runtimes/build:
`make npx deno` · Net/data: `curl wget jq json tar` · Toolchain: `diff prisma next playwright`.
(Bundled, not rebuilt: git docker grep find ls tree.)
Post-reset additions: `ansible-playbook tar gcloud sqlite3 bun ssh rsync deno` (8, hand-built).

### Infrastructure committed
- `SKILL.md` — the `/lowfat` setup+sync entrypoint (agent-driven sync, trust/install boundaries).
- `README.md` (root), `plugins/README.md` (conventions), `templates/lowfat`.
- `scripts/validate.sh` (happy-path) + `scripts/validate.py` (tests.yml-aware, honors exit codes).
- `docs/notes/lowfat-internals.md`, `docs/spec/lowfat-filter-dsl.md` (full DSL ref +
  authoring-pitfalls section), reconciled `docs/spec/pantry-plugin-backlog.md`.

## How the plugins were built (for continuation)
Main session built the structurally-distinct ones with real/representative samples; four
**codex** subagents (sandboxed `workspace-write`) built batches A–D against the DSL spec,
each reviewed/cleaned/validated/committed by the main session. To continue: write a brief
to `/tmp/codex-batch-X.md` mirroring the prior ones, run
`codex exec --sandbox workspace-write -C <repo> - < brief`, then review+commit.

## Docs/packaging pass (2026-06-10, after the audit)
- CLAUDE.md reordered: repo identity first, school/ACE setup last.
- README: skills.sh install bullet (`npx skills add chakrit/lowfat-pantry`); plugin table
  replaced by a 4-line taste + pointer to **`plugins/CATALOG.md`** (new: all 51 plugins
  with per-plugin behavior notes + gotchas, derived from filter.lf headers, coverage
  script-checked against the dirs).
- **De-Clauded**: SKILL.md/README/skill-spec now agent-generic (Claude Code kept as the
  named example where mechanics are real: PreToolUse in `~/.claude/settings.json`).
  GitHub repo description updated via `gh repo edit` to match. decisions/notes left as
  historical records. SKILL.md frontmatter `name:` is now `lowfat-pantry`.
- school-internal `kien-thai`/`visualise` comparison removed from the skill spec.
- Pushed to `gh/main` mid-session (only the final breadcrumb commit may trail).
- ⚠ Still pending from dogfood setup: user-run plugin trust loop (see dogfood section).

## Remaining / next (all LOWER value or LOWER confidence)
- More plugins — only with **real captured samples**: `vercel`/`netlify`/`flyctl`/`gt`
  stay deferred (signal output auth-gated; no capture without accounts). The generic
  `lint/format/test/...` routers stay **deprioritized** — lowfat intercepts real binaries
  only, so a bare `lint` never fires.
- **Enhancement passes** on bundled `git`/`docker` (backlog notes them as basic).
- **Real samples**: many pantry samples are still synthetic (real-captured so far: rg,
  redis-cli, pulumi, wrangler, az, kubectl-noserver, go-test-pass). Replace where a tool +
  fixture project exist — docker containers are a proven cheap path (this session).
- **Test harness (#blocked)**: wire `tests.yml` to `chakrit/smoke` once it's upgraded.
- **Skill polish**: `update-config` integration for the user-scope hook (SKILL.md step 5).

## Hard-won DSL gotchas (now also in docs/spec/lowfat-filter-dsl.md → Authoring pitfalls)
`|` not comma for alternation · no flat-op-then-`match` in one body · `raw`-on-failure
defeats compaction when failure IS the bloat (mvn) · `or-shell:` runs on RAW · never
keyword-filter passthrough output · guard structured output (`--json`) byte-exact · guards
see only `exit ok|failed`.

## Environment notes (for the next session)
Autonomous build ran clean: all writes inside the project tree, cargo registry + school
clone READ-ONLY, never trusted/installed/new, never touched `~/.lowfat`/`~/.config`,
commits on `main` (trunk). NB: `python3` is a venv-expecting shell function on this
machine — run `./scripts/validate.py` as an executable (its `env python3` shebang finds
the real interpreter). The overnight resume cron has already fired and self-deleted.
