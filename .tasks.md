# lowfat-pantry — Session State / Pending Work

Resume point. **chakrit/lowfat-pantry**: a standalone `/lowfat-pantry` skill plus a pantry
of lowfat `.lf` filter plugins, replacing RTK.

## Current state (as of 2026-06-18)
**57 community plugins** built, committed; **smoke golden suite green** (`scripts/test.sh`,
all 57 `tests.cue` UNCHANGED). Inventory + build
posture: `docs/spec/pantry-plugin-backlog.md`; per-plugin detail + gotchas:
`plugins/CATALOG.md`. DSL authoring pitfalls: `docs/spec/lowfat-filter-dsl.md`; test harness:
`docs/spec/smoke-golden-tests.md`. Infra in place: `SKILL.md` entrypoint, root + `plugins/`
READMEs, `templates/lowfat`, `scripts/{test.sh,measure.py}`,
`docs/{spec,notes,decisions}/`. Legacy `tests.yml` + `validate.py` retired this session.
SKILL.md / README / skill-spec are agent-generic (Claude Code only ever a named example).

Repo eats its own dogfood: 9 toolchain-matched plugins symlinked into `~/.lowfat/plugins/`
and trusted; `lowfat hook` wired project-locally in `.claude/settings.local.json`
(gitignored).

## Last session (2026-06-13): docs audit + skill rewrite
- Audited all specs/notes/decisions for staleness:
  - **Pruned** `docs/spec/pantry-plugin-backlog.md` — dropped the pre-build Tier 0–3 tables
    + legend + RTK-0.42 intro (described an unbuilt state, contradicted the prose). Kept
    Built inventory + no-router note + demand-driven posture + RTK-meta mapping. Retitled.
  - **Deleted** `docs/spec/pantry-distribution.md` — superseded Options A/B/C analysis;
    ruling lives in `docs/decisions/2026-06-08-pantry-distribution.md`.
  - **Fixed** `docs/spec/lowfat-skill.md`: status draft→implemented; detect-state now names
    both hook scopes.
  - DSL spec + internals + rtk-migration note + all READMEs: current, left as-is.
- **Rewrote SKILL.md**: why-clauses → terse imperatives (see durable rule below). Kept only
  the non-obvious framing sentences (trust-drift guard, byte-identical-clone no-op,
  permission-surface warning). Fixed Reference: validate.sh→validate.py, added CATALOG.md.

## Last session (2026-06-16): README skill-install lead + fast-path authoring guide
- README: 🥡 callout framing the repo as an agent skill (run `/lowfat-pantry`, agent does
  the work). Corrected overclaims caught on review — binary install is user-run, rewrite
  hook is opt-in/off-by-default (matches SKILL steps 2 & 5). ACE linked to ace-rs.dev.
- SKILL.md: new "Authoring a pantry plugin — fast path" — layout, `.lf` mental model, the
  correctness decision tree (structured/passthrough → `raw` byte-exact; failure-is-bloat
  vs short-error; else scale by level), copy-adapt skeleton, validate step, prompt recipe.
  Lets an agent write a correct filter without the full DSL spec. Commit 176262a.
- **Untested loose end (low pri):** README→SKILL anchor `#authoring-a-pantry-plugin--fast-path`
  assumes GitHub renders the em-dash heading as a double hyphen. Click-test once; if 404,
  drop the em-dash from the heading.

## Last session (2026-06-16/17): uv + npx wrapper dispatch, lf-wishlist
- Trigger: ace-connect peer (`chakrit.เขียนไทย.claude`, autonomous) flagged that `uv run
  pytest`/`uvx ruff` key filter selection on the wrapper word, so pytest/ruff never fire.
  Confirmed vs internals: selection = first token; `lowfat rewrite uv run pytest` returns
  verbatim (no unwrap in 0.6.8).
- **Built `plugins/uv/uv-compact`** (`commands=["uv","uvx"]`) and **enhanced
  `plugins/npx/npx-compact`** the same way: a python dispatcher parses `$args` to detect the
  wrapped tool (uv: run/tool run/uvx/`python -m`; npx: skip `-y`/`-p X`), then applies that
  tool's compaction — pytest/ruff (uv) and eslint/prettier/tsc (npx) **copied** from the
  standalone filters under a drift-contract header. uv also compacts its own
  sync/lock/pip/add; both head-cap unknown tools; bare `npx prettier` + `-f json` kept raw
  (corruption guard). Validated + spot-checked. Plugin count 51→52.
- chakrit chose **full dispatch (duplicate)** both times, knowing the drift. Symlinks can't
  dedup: npx/uv wrap disjoint sets (Node vs Python) so share nothing, and the real axis
  (wrapper reusing the *standalone original*) needs `.lf` include/import, which doesn't
  exist; shell-out + codegen both rejected as worse. Recorded: backlog "Wrapper commands",
  CATALOG.
- **`docs/spec/lf-wishlist.md`** logs upstream engine asks (include/import, wrapper-unwrap,
  `$cmd` exposure, `lowfat filter --plugin`). Posture: log-only, file upstream **only on
  request** (chakrit: "file NO ISSUE, just log it"). include/import filed at **zdk/lowfat#14**
  when asked; the rest unfiled.

## Last session (2026-06-17): smoke v0.3 golden-file test harness
- Trigger: ace-connect peer (`chakrit.smoke.claude`) shipped smoke v0.3.0 — first-class CUE
  specs (closed schema, typo'd field → exit 65), `.json/.jsonl` + `--json`. Unblocks the
  long-standing "wire tests.yml to smoke" item.
- **Architecture (chakrit's call):** smoke is the SOLE judge. validate.py's quality
  heuristics are reborn as a dumb measurement script (`scripts/measure.py`) invoked *inside*
  a smoke command — it prints `lines N`/`bytes N`, never gates; smoke locks the numbers, so
  over-prune/growth regressions surface as drift. Each case locks two commands: raw filter
  (literal golden) + piped-through-measure (metrics).
- **Why CUE:** `_`-hidden fields template the case×level matrix and are dropped on export, so
  the closed schema never sees them — clean templating + closed validation.
- **Rollout:** a one-time generator (since deleted) migrated all 52 `tests.yml`→`tests.cue`;
  tests.cue is now source of truth). Empirical skip filter (commit lock, re-run, drop any
  CHANGED) flagged **zero** nondeterministic plugins — static samples + pure filters → stable
  goldens. 487 tests, `scripts/test.sh` green. Commits 8ea2e4f, ab0be17, 259827c.
- Docs: `docs/spec/smoke-golden-tests.md`; SKILL.md test/layout/reference updated; harness
  docs point at lowfat's own upstream docs (zdk/lowfat PLUGINS/CONFIG), not just fast-path.

## Last session (2026-06-18): output-philosophy doc + ecosystem authorization
- Wrote **`docs/spec/output-philosophy.md`** (commit 1c69953) — codifies the keep-vs-cut
  theory the 52 plugins already embody. Stolen + reconciled from both ancestors:
  - RTK `CONTRIBUTING.md` Design Philosophy: correctness>savings (flag-aware), transparency
    (filtered = valid *subset*), never-block (fallback arm), recovery-hint-on-truncation,
    zero-overhead. Plus in/out scope + four strategies (filter/group/truncate/dedup).
  - lowfat `docs/PLUGINS.md` filter contract: ultra=verdict / full=strip-chatter-keep-
    structure / lite=gentle / exit≠0=conservative / empty=passthrough; ≥80% at full.
  - **Pantry invariants** (where we're stricter): byte-exact structured (not just "subset"),
    passthrough-bodies-cap-only, never-hide-errors, **exit-code-is-signal-not-failure-proxy**,
    over-prune-is-drift. Plus the **ultra-breaks-transparency-WITH-markers** exception.
  - Scope: tight (principles+invariants, ~1pg). NOT an essay, NOT absorbing the fast path.
- **Absorbed into SKILL.md fast path**: exit-code-is-signal caveat (was missing), recovery-
  hint rule on list caps, bidirectional cross-links (fast-path↔philosophy, Reference). Listed
  in CLAUDE.md source-of-truth. No filter touched → no smoke run needed.
- **chakrit authorized proactive ecosystem coverage** (overrides demand-driven posture for
  this push). Decision: target = **Ruby + deepen go/dotnet/mvn/gradlew**.

## Last session (2026-06-18, cont'd): Ruby ecosystem + dotnet deepen — DONE (AFK)
chakrit ran AFK ("/ace-afk" — no such skill exists; honored as autonomous loop, no
approval gate, commit at seams, no push). Extended plugin set delivered:
- **Ruby set (5 plugins, commit ab61d06):** `rspec` `rubocop` `bundle` `gem` `rake`.
  Real samples docker-captured from `ruby:3.3-slim` (+`build-essential` for native gems —
  slim alone fails `json`/`prism` builds; bundle install FIRST for real resolver chatter,
  not pre-`gem install`d). rspec≈pytest (pass→tally, fail→Failures/rerun/summary), rubocop≈
  ruff (offense lines + tally, drop caret/Tip, clean→verdict, bad-path→raw). bundle install
  collapses to "Bundle complete!"; failed resolve drops the 300-line matching-gems version
  dump. gem/rake use a shared `cap(N)` macro (head + "... (N lines total)" hint). rake +
  bundle-exec are passthrough bodies (cap only). **rspec/rubocop guard `--format/-f json`
  → raw** (invariant 1; real JSON samples locked — note lowfat appends a trailing `\n` to
  ALL output incl. raw, so "byte-exact" = content-identical, +1 newline; JSON stays valid).
- **dotnet deepen (commit bee5f20):** `build|test` → `build|test|publish|pack`. Same output
  shape; added `Successfully created package` to keep-list (pack verdict); publish's terse
  path lines use the existing or-shell fallback. Real `dotnet/sdk:8.0` samples.
- **go/mvn/gradlew assessed ADEQUATE, no change** (don't re-litigate): mvn + gradlew are
  deliberately phase/task-agnostic single-`*`-rule designs (phases/tasks are args, not
  subcommands) — no subcommand depth to add. go's only gaps (`get`/`install` download
  chatter) are handled safely by the `*` catch-all (cap-on-success/raw-on-failure); a
  dedicated rule would be polish, not a fix → leave demand-driven.
- Docker hygiene: pulled ruby + dotnet images and built ephemeral `--rm` containers; both
  images `docker rmi`'d, tmpdirs removed, zero `lf-*` containers left. Net disk ≈flat
  (~1GB transient VM slack, reclaims itself; did NOT touch chakrit's 8.5GB shared build
  cache or other images).

## NEXT TASK: none queued — extended set complete; back to demand-driven
The proactive ecosystem push chakrit authorized (Ruby + deepen) is DONE. Posture reverts to
demand-driven: build/enhance only on observed real-session bloat (see below).

## Durable rule established this session
**Skill house style: imperatives over why-clauses.** skill-creator's "explain the why"
advice doesn't move model behavior — agents skim why-clauses; the description is the only
triggering lever, and a loaded skill is followed, not deliberated against. Source:
prod9/school decision `2026-04-26-skill-house-style-overrides-skill-creator`. Now also in
user global CLAUDE.md (`## Skills`) so it reaches standalone repos, not just school skills.

## Remaining / next — demand-driven, NONE is "just do it"
- Build/enhance only on observed real-session bloat (full posture: backlog doc → Build
  posture). Do NOT re-propose plugins on a planning pass.
- New/changed plugins now get a `tests.cue` golden (lock via `smoke -c`, review the diff);
  harness: `docs/spec/smoke-golden-tests.md`. No more `tests.yml`/`validate.py`.
- **Upstream (zdk/lowfat)** — `lowfat hook` returns `permissionDecision:"allow"`, auto-
  approving every filtered cmd past the agent prompt. Fix: emit `updatedInput` *without*
  `permissionDecision` (hook.rs:31-42). SKILL.md step 5 warns users until then.
- **Upstream (zdk/lowfat) — engine asks** — full list + proposed shapes in
  `docs/spec/lf-wishlist.md`: `include`/`import` (**filed: zdk/lowfat#14**), wrapper-unwrap
  (kills the uv/npx dispatch duplication), `$cmd` exposure, `lowfat filter --plugin`.
  Log-only; file the rest upstream **only if chakrit asks**.

## Continuation method (only if a real need surfaces)
Structurally-distinct plugins built in the main session with real samples; bulk via **codex**
subagents (`codex exec --sandbox workspace-write -C <repo> - < brief`), each reviewed/
validated/committed by main. Audit via Explore-agent fan-out, but hand-verify — ~half of
findings last time were false positives.

## Environment notes
`python3` is a venv-expecting shell function — run repo python scripts (`measure.py`) as
executables, not via `python3 <path>`.
Commits go on `main` (trunk). Docker is the cheap real-capture path; remove `lf-*` containers
+ any session-pulled images afterward.
