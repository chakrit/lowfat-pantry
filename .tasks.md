# lowfat-pantry — Session State / Pending Work

Resume point for the next `/ace`. This repo is becoming **chakrit/lowfat-pantry**: a
standalone Claude Code `/lowfat` skill plus a pantry of lowfat plugins/filters. Design is
settled and committed under `docs/`; build has not started. Working tree clean.

## Where we stopped

Design + docs phase complete, no code/plugins yet. Two decisions wait on chakrit; two tasks
are externally blocked. The unblocked critical path is the skill scaffold (#4).

## Pending tasks

- **#4 — Restructure repo as the standalone `/lowfat` skill repo. READY (critical path).**
  Build `SKILL.md` (arc in `docs/spec/lowfat-skill.md`), `templates/lowfat` (no dot), and a
  `plugins/` scaffold. No `bin/sync` — sync is agent-driven per the spec. Skill resolves its
  own install dir as the symlink source.
- **#3 — Build Tier-0/1 pantry plugins. BLOCKED on chakrit.** Needs the backlog re-rank +
  fold calls (`prettier→format`, `vitest/jest→test`) in `docs/spec/pantry-plugin-backlog.md`.
- **#2 — Build smoke-based plugin test harness. BLOCKED on upgraded `chakrit/smoke`** (a
  separate agent is upgrading it). Golden + invariant via `tests.yml` over
  (sample × lite/full/ultra) using `lowfat filter --level`.
- **#5 — File upstream ask: additive `LOWFAT_PLUGIN_PATH` to zdk/lowfat.** Low priority;
  would erase the symlink-sync step entirely.

## Open decisions for chakrit

1. Backlog prioritization + fold calls — unblocks #3.
2. Build order once unblocked: `SKILL.md` (#4) first vs first plugin (#3). Lean: #4 first
   (the rail before the cargo).

## Done this session (all committed)

- `docs/` scaffolded (notes/decisions/spec) via ace-docs.
- Distribution decided — standalone skill repo, agent-sync (no script), ACE = one delivery
  channel: `docs/decisions/2026-06-08-pantry-distribution.md`.
- `/lowfat` skill arc drafted: `docs/spec/lowfat-skill.md`.
- `.lowfat` project config committed (`level=full`).
- Naming/design decoupled from ACE (`lowfat-pantry`, not `lowfat-ace`).

## Key context for a fresh session

- lowfat: single override-only plugin home (`~/.config/lowfat/plugins/`), name-based trust
  (not content-hash), no additive search path. Levels: lite / full / ultra.
- The school's `rtk` skill is reference only, **not** authoritative — the `/lowfat` skill is
  derived from lowfat's own mechanics.
- smoke = `chakrit/smoke` golden-file tester (`tests.yml`, `smoke -c` to accept snapshots).
- RTK command surface (67 cmds) is the plugin-candidate source for the backlog.
